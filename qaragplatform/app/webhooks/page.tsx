'use client'
import PlaceholderPage from '@/components/PlaceholderPage'
import { Webhook } from 'lucide-react'
export default function WebhooksPage() {
  return <PlaceholderPage icon={Webhook} title="Webhooks" description="Configure webhooks to receive real-time notifications when documents are processed or queries are answered." />
}
